<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <artifactId>camunda-engine-rest-openapi-generator</artifactId>
  <name>Camunda Platform - engine - REST - OpenAPI (JSON Generator)</name>
  <packaging>jar</packaging>

  <parent>
    <groupId>org.camunda.bpm</groupId>
    <artifactId>camunda-engine-rest-root</artifactId>
    <relativePath>../</relativePath>
    <version>7.18.0-alpha5</version>
  </parent>
  
  <properties>
    <!-- The JSON schema validator works with Jackson 2.12 currently, but not with the latest patch level.
         This can potentially be removed if the validator uses a proper patch level version of Jackson as well -->
    <version.jackson>2.12.6.20220326</version.jackson>
    <version.json-schema-validator>1.0.69</version.json-schema-validator>
  </properties>

  <dependencies>

    <dependency>
      <groupId>org.freemarker</groupId>
      <artifactId>freemarker</artifactId>
    </dependency>

    <!-- json formatting -->
    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
    </dependency>

    <!-- file management -->
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
    </dependency>

    <!-- json schema validation -->
    <dependency>
      <groupId>com.networknt</groupId>
      <artifactId>json-schema-validator</artifactId>
      <version>${version.json-schema-validator}</version>
    </dependency>

  </dependencies>

</project>