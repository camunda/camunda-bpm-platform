<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <artifactId>fox-platform-qa-tomcat6-runtime</artifactId>
  <packaging>pom</packaging>

  <name>fox platform EE - QA Tomcat 6 Test Runtime</name>  

  <parent>
    <groupId>com.camunda.fox.platform</groupId>
    <artifactId>fox-platform-qa</artifactId>
    <version>7.0.0-SNAPSHOT</version>
  </parent>

  <dependencies>
    <dependency>
      <!-- make sure it comes after assembly in the build -->
      <groupId>com.camunda.fox.platform</groupId>
      <artifactId>fox-platform-tomcat6-assembly</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>   
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main</directory>
        <filtering>true</filtering>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-distro</id>
            <phase>package</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target unless="maven.test.skip">
                <copy todir="target/apache-tomcat-6.0.36">
                  <fileset dir="${tomcat6.assembly.location}" />
                </copy>                
              </target>
            </configuration>
          </execution>          
        </executions>
      </plugin>
      <!-- copy filtered configuration resources -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <configuration>
          <delimiters>
            <delimiter>${*}</delimiter>          
          </delimiters>
          <useDefaultDelimiters>false</useDefaultDelimiters>
        </configuration>
        <executions>
          <execution>
            <id>copy-resources</id>
            <phase>package</phase>
            <goals>
              <goal>resources</goal>
            </goals>
            <configuration>
              <override>true</override>
              <outputDirectory>target/apache-tomcat-6.0.36</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
