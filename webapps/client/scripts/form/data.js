'use strict';
if (typeof define !== 'function') { var define = require('amdefine')(module); }
/* jshint unused: false */
define([
           'angular'
], function(angular) {
  var formDataModule = angular.module('cam.tasklist.form.data', []);

  return formDataModule;
});
