<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.camunda.fox.tasklist</groupId>
    <artifactId>tasklist-root</artifactId>
    <version>6.2.0-alpha4</version>
  </parent>

  <artifactId>tasklist-webapp</artifactId>
  <packaging>war</packaging>

  <name>fox tasklist - Webapp</name>
  <description>
		Central tasklist application in the fox platform being able to work together with Process Applications which only deploy forms.
		See https://app.camunda.com/confluence/display/foxUserGuide/fox+tasklist for details.
	</description>

  <dependencies>
    <dependency>
      <groupId>com.camunda.fox.tasklist</groupId>
      <artifactId>tasklist-core</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- include jar to enable different authentication mechanisms, e.g. 
      LDAP. Additionaly you have to set the implementation class as CDI alternative 
      via the file src/main/webapp/WEB-INF/beans.xml <dependency> <groupId>com.camunda.fox.demo.outerspace</groupId> 
      <artifactId>tasklist-ldap-integration</artifactId> <version>6.0.2-SNAPSHOT</version> 
      </dependency> -->

    <dependency>
      <groupId>com.camunda.fox.platform</groupId>
      <artifactId>fox-platform-client</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>com.camunda.fox.platform</groupId>
      <artifactId>fox-platform-api</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>com.camunda.fox.engine</groupId>
      <artifactId>fox-engine</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>com.camunda.fox.tasklist</groupId>
      <artifactId>tasklist-layout</artifactId>
      <version>${project.version}</version>
      <type>war</type>
    </dependency>

    <!-- Java EE 6 Specification -->
    <dependency>
      <groupId>org.jboss.spec</groupId>
      <artifactId>jboss-javaee-web-6.0</artifactId>
      <version>2.0.0.Final</version>
      <type>pom</type>
      <scope>provided</scope>
      <exclusions>
        <exclusion>
          <artifactId>xalan</artifactId>
          <groupId>org.apache.xalan</groupId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- test dependencies -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.8.2</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.jboss.arquillian.junit</groupId>
      <artifactId>arquillian-junit-container</artifactId>
      <version>1.0.0.CR7</version>
      <scope>test</scope>
    </dependency>

    <!-- Requires a running JBoss 7 with fox platform change to other artifact 
      for embedded start or different server -->
    <dependency>
      <groupId>org.jboss.as</groupId>
      <artifactId>jboss-as-arquillian-container-remote</artifactId>
      <version>7.1.0.Final</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>1.2.132</version>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <finalName>tasklist</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <configuration>
          <failOnMissingWebXml>false</failOnMissingWebXml>
          <overlays>
            <overlay>
              <groupId>com.camunda.fox.tasklist</groupId>
              <artifactId>tasklist-layout</artifactId>
            </overlay>
          </overlays>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>